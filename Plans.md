# Upcoming plans
- Include a secondary source, im currently thinking https://mangadex.org/
- Simplify the search, possibly do it amonst any source we have implemented and have the user select which source they want to add to their data file
- Move this directed more towards a CLI tool, that you can run a single command to search/add, download, etc, instead of a main call menu
- Fail fast if env config is missing

- Refactor to fail properly, give more accurate crash logs, and information about what could have went wrong. -> input validation like on view_manga_data, update_manga_data etc
- 
